import type { Svg } from "react-native-svg";

/**
 * Propiedades do ícone UndrawRelaxedReading, baseado na paleta de cores do tema.
 * @example
 * ```tsx
 * <UndrawRelaxedReading />
 * ```
 */
export type UndrawRelaxedReadingProps = React.ComponentProps<typeof Svg> & {
  /** Altura do ícone */
  height?: number;
  /** Largura do ícone */
  width?: number;
};
